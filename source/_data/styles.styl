// ==========================================
// 庹林博客专属样式 - 注入灵魂
// ==========================================

// 1. 设置侧边栏顶部（Site Meta）的背景图
// 这里就是“生如逆旅”那几个字的背景
.site-brand-container {
    // 替换下面的文件名为你上传的那张“一苇渡江”图的文件名
    background: url('https://img.laotuo.top/1764939671822.jpg') no-repeat center center;
    background-size: cover;
}

// 2. 为了让白色的文字看清，加一层半透明黑遮罩
.site-brand-container::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0,0,0,0.4); // 40% 的黑色遮罩
    z-index: 0;
}

// 3. 确保文字在遮罩上层
.site-meta {
    z-index: 1;
    position: relative;
}

// 4. 强制网站标题颜色为白色
.site-title {
    color: #ffffff !important;
    font-weight: bold;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.8); // 加一点阴影更硬朗
}
